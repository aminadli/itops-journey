### What We Did

- Installed Oracle VirtualBox on a host machine
- Created a VM with 4 vCPU, 8 GB RAM, 100 GB disk
- Installed Ubuntu Server (minimal)
- Set a static IP,
- Created user
- Enabled OpenSSH during install

### Difficulties

| Problem | Cause | Fix |
|---|---|---|
| cannot login | — | Delete the vm and restart the process and choose manual user setup |

### What We Learned

For some reason I have issue with unattended setup as i key the user and password that i key in earlier but it kept failing so i revert untick unattended setup and choose to add user manually
